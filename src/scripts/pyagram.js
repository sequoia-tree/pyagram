export function drawPyagram(pyagram) {
    switch (pyagram.encoding) {
        case 'pyagram':
            // TODO
            // pgSnapshots = pyagram.data.snapshots;
            // pgSlider.min = 0;
            // pgSlider.max = pgSnapshots.length - 1;
            // slider.resetSlider(pgSlider);
            break;
        case 'syntax_error':
            // TODO
            break;
        case 'pyagram_error':
            // TODO
            break;
        default:
            // TODO
            break;
    }
}
